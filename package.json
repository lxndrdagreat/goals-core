{
  "name": "goals-core",
  "version": "1.0.0",
  "description": "Core schema for the Goals app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/tsc --project ./tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "files": [
    "lib/**/*"
  ],
  "author": "Dan Alexander <lxndrdagreat@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.1.3"
  }
}
